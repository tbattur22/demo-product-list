const Footer = ({styles, curStyle, setStyle}) => {
    // console.log(`Footer: styles: ${styles}, curStyle: ${curStyle}, setStyle: ${setStyle}`);

    return (
        <div className="">
            <h3>Footer goes here.</h3>
        </div>
    )
}

export default Footer;